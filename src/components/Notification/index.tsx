import { NotificationDropdown } from "./notification-dropdown";

// The old Notification component is no longer needed,
// as NotificationDropdown handles everything.
// We export it under the name "Notification" for backward compatibility in other files.
export const Notification = NotificationDropdown;
